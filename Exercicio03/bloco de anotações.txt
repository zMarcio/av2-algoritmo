Prompt: Como posso conseguir com esse metodo que se baseia no partition do quicksort fazer esse desafio Escrever um algoritmo de reorganização desse vetor obedecendo a ordem: crianças de 14 a 16 anos; crianças de 11 a 13 anos; e crianças de 6 a 10 anos. Seu algoritmo de reorganização deve executar em tempo O(n) e ser local. Escrever a saída do seu programa após a reorganização do vetor. Realizar a análise de complexidade do seu algoritmo.
        
resolução encontrada dutch national flag algorithm
        
        // crianças de 14 a 16 anos; crianças de 11 a 13 anos; e crianças de 6 a 10 anos.
		int pivot14 = 14;
		int pivot11 = 11;
		int pivot6 = 6;
		int i = -1;
        int j = 0;
        int p;
		Crianca temp;
		for(int j = 0; j < A.length; j++){

			if(A[j].idade >= pivot14){
				i++;
				temp = A[i];
				A[i] = A[j];
				A[j] = temp;
			}else if(A[j].idade >= pivot11){
				i++;
				temp = A[i];
				A[i] = A[j];
				A[j] = temp;
			}
			else{
				i++;
				temp = A[i];
				A[i] = A[j];
				A[j] = temp;
			}

		}